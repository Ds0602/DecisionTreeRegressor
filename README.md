# DecisionTreeRegressor
Custom Decision Tree Regressor built from scratch in Python with common libraries such as pandas and numpy, featuring manual split logic, support for numerical and categorical features, and both optimal and randomized splitting. Explores trade-offs between accuracy, efficiency, and feature encoding.
